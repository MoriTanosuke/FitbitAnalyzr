<% if session[:user_id] != nil %>
  <% if User.find_by_id(session[:user_id]).fitbit == nil %>
    <p><%= image_tag "/logos/fitbit_logo_dark_100px.png" %> <%= button_to 'Connect to fitbit', oauth_consumers_path + "/fitbit", :method => 'get' %></p>
  <% else %>
    <p><%= image_tag "/logos/fitbit_logo_dark_100px.png" %></p>
    <p><%= button_to 'Disconnect', users_path + "/forget_fitbit" %></p>
  <% end %>
<% end %>

<h2>What you can do:</h2>
<ul>
<li>Login/Logout</li>
<li>Connect to fitbit</li>
<li>Disconnect fitbit</li>
<li>Import sleep</li>
</ul>

<h2> What you can't do (yet):</h2>
<ul>
<li>Import all other data</li>
<li>Apply tags to days</li>
<li>Display several charts into one huge chart</li>
</ul>

